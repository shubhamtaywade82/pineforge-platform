name: Security

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]
  schedule:
    - cron: '0 6 * * 1'

jobs:
  dependency-review:
    name: Dependency review placeholder
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Security scan placeholder
        run: echo "Security scans begin once dependencies are introduced at m01."
